{{- define "database.secret" -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
data:
  mysql-root-password: {{ .Values.credentials.rootPassword | b64enc }}
  {{- end -}}